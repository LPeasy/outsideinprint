{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML -}}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- range sort (where site.Pages "Kind" "in" (slice "home" "page" "section")) "Permalink" -}}
    {{- $policy := partial "metadata/policy.html" (dict "page" .) -}}
    {{- if $policy.in_sitemap -}}
      {{- $lastmod := partial "metadata/sitemap-lastmod.html" . -}}
      <url>
        <loc>{{ .Permalink }}</loc>
        {{- with $lastmod }}
          <lastmod>{{ . }}</lastmod>
        {{- end }}
      </url>
    {{- end -}}
  {{- end -}}
</urlset>
